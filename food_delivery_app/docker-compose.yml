version: '3.8'

services:
  smart-soko:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      # Firebase credentials (replace with actual values or use Docker secrets)
      - FIREBASE_PROJECT_ID=fooddelievry-dce15
      - FIREBASE_PRIVATE_KEY=
      - FIREBASE_CLIENT_EMAIL=
      # Supabase credentials
      - SUPABASE_URL=https://vonkqyiczeqhuqhahsxm.supabase.co
      - SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvbmtxeWljemVxaHVxaGFoc3htIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQ4MjIzNDksImV4cCI6MjA5MDM5ODM0OX0.UKAT3re6P_oAB3E1svwCFdqTQWZL6yulJ1ZX4nAgJJ8
      # Other optional environment variables
      - FIREBASE_SERVICE_ACCOUNT=serviceAccountKey.json
    volumes:
      # Mount service account key if available locally
      - ./serviceAccountKey.json:/app/serviceAccountKey.json:ro
    restart: unless-stopped